(function (global, factory) {
	typeof exports === 'object' && typeof module !== 'undefined' ? module.exports = factory() :
	typeof define === 'function' && define.amd ? define(factory) :
	(global.ssm = factory());
}(this, (function () { 'use strict';

function filterStates(states, key, value) {
    var len = states.length;
    var returnStates = [];

    for (var i = 0; i < len; i++) {
        var state = states[i];

        if (state[key] && state[key] === value) {
            returnStates.push(state);
        }
    }

    return returnStates;
}

function mergeOptions(obj1, obj2) {
    var obj3 = {};

    for (var attrname in obj1) {
        obj3[attrname] = obj1[attrname];
    }

    for (var attrname2 in obj2) {
        obj3[attrname2] = obj2[attrname2];
    }

    return obj3;
}

function makeID() {
    var text = "";
    var possible = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789";

    for (var i = 0; i < 10; i++) {
        text += possible.charAt(Math.floor(Math.random() * possible.length));
    }
    return text;
}

function fireAllMethodsInArray(arr) {
    var arrLength = arr.length;

    for (var i = 0; i < arrLength; i++) {
        arr[i]();
    }
}



function debounce(func, wait, immediate) {
    var timeout;

    return function () {
        var context = this,
            args = arguments;
        var later = function later() {
            timeout = null;
            if (!immediate) func.apply(context, args);
        };
        var callNow = immediate && !timeout;
        clearTimeout(timeout);
        timeout = setTimeout(later, wait);
        if (callNow) func.apply(context, args);
    };
}

function _classCallCheck$1(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

var configOptions = [];
var stateChangeMethod = function stateChangeMethod() {};

var State = function () {
    function State(options) {
        _classCallCheck$1(this, State);

        this.id = options.id || makeID();
        this.query = options.query || 'all';
        // These are exposed as part of the state, not options so delete before
        // we merge these into default options.
        delete options.id;
        delete options.query;

        var defaultOptions = {
            onEnter: [],
            onLeave: [],
            onResize: [],
            onFirstRun: []
        };

        //Merge options with defaults to make the state
        this.options = mergeOptions(defaultOptions, options);

        //Migrate methods into an array, this is to enable future functionality of adding extra methods to an existing state
        if (typeof this.options.onEnter === "function") {
            this.options.onEnter = [this.options.onEnter];
        }

        if (typeof this.options.onLeave === "function") {
            this.options.onLeave = [this.options.onLeave];
        }

        if (typeof this.options.onResize === "function") {
            this.options.onResize = [this.options.onResize];
        }

        if (typeof this.options.onFirstRun === "function") {
            this.options.onFirstRun = [this.options.onFirstRun];
        }

        //Test the one time tests first, if the test is invalid we wont create the config option
        if (this.testConfigOptions('once') === false) {
            this.valid = false;
            return false;
        }

        this.valid = true;
        this.active = false;
        this.init();
    }

    State.prototype.init = function init() {
        this.test = window.matchMedia(this.query);

        if (this.test.matches && this.testConfigOptions('match')) {
            this.enterState();
        }

        this.listener = function (test) {
            var changed = false;

            if (test.matches) {
                if (this.testConfigOptions('match')) {
                    this.enterState();
                    changed = true;
                }
            } else {
                this.leaveState();
                changed = true;
            }

            if (changed) {
                stateChangeMethod();
            }
        }.bind(this);

        this.test.addListener(this.listener);
    };

    //Handle entering a state


    State.prototype.enterState = function enterState() {
        fireAllMethodsInArray(this.options.onFirstRun);
        fireAllMethodsInArray(this.options.onEnter);
        this.options.onFirstRun = [];
        this.active = true;
    };

    //Handle leaving a state


    State.prototype.leaveState = function leaveState() {
        fireAllMethodsInArray(this.options.onLeave);
        this.active = false;
    };

    //Handle the user resizing the browser


    State.prototype.resizeState = function resizeState() {
        if (this.testConfigOptions('resize')) {
            fireAllMethodsInArray(this.options.onResize);
        }
    };

    //When the StateManager removes a state we want to remove the event listener


    State.prototype.destroy = function destroy() {
        this.test.removeListener(this.listener);
    };

    State.prototype.attachCallback = function attachCallback(type, callback, runIfActive) {
        switch (type) {
            case 'enter':
                this.options.onEnter.push(callback);
                break;
            case 'leave':
                this.options.onLeave.push(callback);
                break;
            case 'resize':
                this.options.onResize.push(callback);
                break;
        }

        if (type === 'enter' && runIfActive && this.active) {
            callback();
        }
    };

    State.prototype.testConfigOptions = function testConfigOptions(when) {
        var totalConfigOptions = configOptions.length;

        for (var j = 0; j < totalConfigOptions; j++) {
            var configOption = configOptions[j];

            if (typeof this.options[configOption.name] !== "undefined") {
                if (configOption.when === when && configOption.test.bind(this)() === false) {
                    return false;
                }
            }
        }

        return true;
    };

    State.addConfigOption = function addConfigOption(configOption) {
        configOptions.push(configOption);
    };

    State.getConfigOptions = function getConfigOptions() {
        return configOptions;
    };

    State.removeConfigOption = function removeConfigOption(name) {
        configOptions.forEach(function (item, index) {
            if (item.name === name) {
                configOptions.splice(index, 1);
            }
        });
    };

    State.setStateChangeMethod = function setStateChangeMethod(func) {
        if (typeof func === "function") {
            stateChangeMethod = func;
        } else {
            throw new Error('Not a function');
        }
    };

    return State;
}();

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

var resizeTimeout = 25;

//State Manager Constructor

var StateManager = function () {
    function StateManager(options) {
        _classCallCheck(this, StateManager);

        this.states = [];
        this.resizeTimer = null;
        this.configOptions = [];

        window.addEventListener("resize", debounce(this.resizeBrowser.bind(this), resizeTimeout), true);
    }

    StateManager.prototype.addState = function addState(options) {
        var newState = new State(options);

        if (newState.valid) {
            this.states.push(newState);
        }

        return newState;
    };

    StateManager.prototype.addStates = function addStates(statesArray) {
        for (var i = statesArray.length - 1; i >= 0; i--) {
            this.addState(statesArray[i]);
        }

        return this;
    };

    StateManager.prototype.getState = function getState(id) {
        for (var i = this.states.length - 1; i >= 0; i--) {
            var state = this.states[i];

            if (state.id === id) {
                return state;
            }
        }
    };

    StateManager.prototype.isActive = function isActive(id) {
        var selectedState = this.getState(id) || {};

        return selectedState.active || false;
    };

    StateManager.prototype.getStates = function getStates(idArr) {
        var idCount = null,
            returnArr = [];

        if (typeof idArr === "undefined") {
            return this.states;
        } else {
            idCount = idArr.length;

            for (var i = 0; i < idCount; i++) {
                returnArr.push(this.getState(idArr[i]));
            }

            return returnArr;
        }
    };

    StateManager.prototype.removeState = function removeState(id) {
        for (var i = this.states.length - 1; i >= 0; i--) {
            var state = this.states[i];

            if (state.id === id) {
                state.destroy();
                this.states.splice(i, 1);
            }
        }

        return this;
    };

    StateManager.prototype.removeStates = function removeStates(idArray) {
        for (var i = idArray.length - 1; i >= 0; i--) {
            this.removeState(idArray[i]);
        }

        return this;
    };

    StateManager.prototype.removeAllStates = function removeAllStates() {
        for (var i = this.states.length - 1; i >= 0; i--) {
            var state = this.states[i];
            state.destroy();
        }

        this.states = [];
    };

    StateManager.prototype.addConfigOption = function addConfigOption(options) {
        var defaultOptions = {
            name: '', // name, this is used to apply to a state
            test: null, //function which will perform the test
            when: 'resize' // resize or match (match will mean that resize will never fire either), or once (which will test once, then delete state if test doesnt pass)
        };

        //Merge options with defaults
        options = Object.assign({}, defaultOptions, options);

        if (options.name !== '' && options.test !== null) {
            State.addConfigOption(options);
        }
    };

    StateManager.prototype.removeConfigOption = function removeConfigOption(name) {
        State.removeConfigOption(name);
    };

    StateManager.prototype.getConfigOption = function getConfigOption(name) {
        var configOptions = State.getConfigOptions();

        if (typeof name === "string") {
            for (var i = configOptions.length - 1; i >= 0; i--) {
                if (configOptions[i].name === name) {
                    return configOptions[i];
                }
            }
        } else {
            return configOptions;
        }
    };

    StateManager.prototype.getConfigOptions = function getConfigOptions() {
        return State.getConfigOptions();
    };

    StateManager.prototype.resizeBrowser = function resizeBrowser() {
        var activeStates = filterStates(this.states, 'active', true);
        var len = activeStates.length;

        for (var i = 0; i < len; i++) {
            activeStates[i].resizeState();
        }
    };

    StateManager.prototype.stateChange = function stateChange(func) {
        State.setStateChangeMethod(func);
    };

    return StateManager;
}();



var ssm = new StateManager();

return ssm;

})));
//# sourceMappingURL=ssm.min.js.map
